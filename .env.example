# No environment variables are required for the current practice scaffold.
# Future API or data configuration can be documented here when it is introduced.
# The frontend currently defaults to http://localhost:8000 for the local backend.
